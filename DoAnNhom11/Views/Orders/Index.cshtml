@{
    ViewData["Title"] = "Order";
    Layout = "~/Views/Shared/_MyLayout.cshtml";
    ViewBag.Action = "Details";
}
@using X.PagedList.Mvc.Core;
@model X.PagedList.IPagedList<Order>;
<h1 style="text-align:center">Đơn hàng của tôi</h1>
@foreach (var item in Model)
{

        @Html.Partial("_OrderPartial", item)
}
@* <table class="table">
    <thead>
        <tr>
            <th>
                Ngày Đặt
            </th>
            <th>
                Tổng tiền
            </th>
            <th>
                Địa chỉ giao hàng
            </th>
            <th>
                Ghi chú
            </th>
            <th>
                Trạng thái đơn hàng
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @StringFormat.DateFormat(item.OrderDate)
                </td>
                <td>
                    @StringFormat.CurrencyFormat(item.TotalPrice)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ShippingAddress)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Notes)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.OrderStatus.TenTrangThai)
                </td>
                <td>
                    <button type="button" class="btn btn-fefault cart">
                        <a asp-action="Details" asp-route-ma="@item.OrderId" style="color:white" onmouseover="this.style.color='black'" onmouseout="this.style.color='white'">Xem chi tiết</a>
                    </button>
                </td>
                <td>
                    @if (item.OrderStatusId <= 2)
                    {
                        <form asp-action="Delete" asp-controller="Orders" asp-route-id="@item.OrderId" method="post">
                            <input type="submit" value="Hủy Đơn" onclick="return confirm('Bạn Có Chắc Chắn Muốn Hủy Đơn Này?');" class="btn btn-danger" />
                        </form>
                    }
                </td>
            </tr>
        }
    </tbody>
</table> *@
<center>
    <ul class="pagination">
        @Html.PagedListPager(Model, page => Url.Action("Index", new { page = page }))
    </ul>
</center>